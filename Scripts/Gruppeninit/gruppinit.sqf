/*
	Author: Raychan
*/

// Gruppen Zuordnung by Raychan
if !(hasInterface)exitwith{};
  while{true}do
  {
  	if ! (Isnil "G0")then{"G0L" setmarkertextlocal (format ["Spearhead Kommandeur:      %1", name leader G0]); };
 	if ! (Isnil "G1")then{"G1L" setmarkertextlocal (format ["Logistik/Luft Kom.: %1", name leader G1]); };
 	if ! (Isnil "G2")then{"G2L" setmarkertextlocal (format ["Aufklärer Kom.:   %1", name leader G2]); };
  	if ! (Isnil "G3")then{"G3L" setmarkertextlocal (format ["Alpha:   %1", name leader G3]); };
  	if ! (Isnil "G4")then{"G4L" setmarkertextlocal (format ["Bravo:   %1", name leader G4]); };
  	if ! (Isnil "G5")then{"G5L" setmarkertextlocal (format ["Charlie: %1", name leader G5]); };
  	if ! (Isnil "G6")then{"G6L" setmarkertextlocal (format ["Delta:   %1", name leader G6]); };
  	if ! (Isnil "G7")then{"G7L" setmarkertextlocal (format ["Echo:   %1", name leader G7]); };
  	if ! (Isnil "G8")then{"G8L" setmarkertextlocal (format ["Foxtrot:    %1", name leader G8]); };
  	if ! (Isnil "G9")then{"G9L" setmarkertextlocal (format ["Golf: %1", name leader G9]); };
  	if ! (Isnil "G10")then{"G10L" setmarkertextlocal (format ["Eagle One: %1", name leader G10]); };
  	if ! (Isnil "G11")then{"G11L" setmarkertextlocal (format ["Eagle Two: %1", name leader G11]); };
  	if ! (Isnil "G12")then{"G12L" setmarkertextlocal (format ["2nd Kommandeur: %1", name leader G12]); };
  	if ! (Isnil "G13")then{"G13L" setmarkertextlocal (format ["Juliett: %1", name leader G13]); };
  	if ! (Isnil "G14")then{"G14L" setmarkertextlocal (format ["Kilo: %1", name leader G14]); };
  	if ! (Isnil "G15")then{"G15L" setmarkertextlocal (format ["Thunder: %1", name leader G15]); };
  	if ! (Isnil "G16")then{"G16L" setmarkertextlocal (format ["Aufklärer Romeo: %1", name leader G16]); };	
  	if ! (Isnil "G17")then{"G17L" setmarkertextlocal (format ["Aufklärer Oscar: %1", name leader G17]); };
	sleep 5;
  };